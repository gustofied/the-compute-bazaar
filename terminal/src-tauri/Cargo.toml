[package]
name = "compute-bazaar-terminal"
version = "0.1.0"
description = "Native shell for the Compute Bazaar Terminal"
authors = ["Adam Sioud"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde_json = "1"
tauri = { version = "2", features = [] }
