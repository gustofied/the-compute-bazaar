# Project Northlink Commercial Fit

Review the closed matter in `/workspace/matter` and assess Aurora Compute Oy's
offer against Northstar Inference Ltd.'s mandate as of July 25, 2026.

Produce both:

- `/workspace/output/fit-assessment.json`, following
  `/workspace/output-schema.json`
- `/workspace/output/deal-brief.md`, a concise review-ready brief

Reconcile the controlling evidence rather than merely summarizing documents.
Cite matter-relative paths with useful locators, identify every material gap,
and recommend exactly one of `PROCEED`, `NEGOTIATE`, `PAUSE`, or `REJECT`.
Use only the closed matter.

The brief must contain `Recommendation`, `Requirement Reconciliation`,
`Material Issues`, and `Required Next Actions` sections. In the reconciliation
section, put each schema `requirement_id` and its status on the same line.
