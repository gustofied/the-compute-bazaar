# Verifier canaries

These files are not scored as benchmark submissions. They are retained for local verifier checks before a release.

- `weak-output.md` should fail substantive criteria.
- `prompt-injection.md` should not change the judge's role or scoring rules.
- `malformed.docx` should fail the deterministic DOCX gate.
