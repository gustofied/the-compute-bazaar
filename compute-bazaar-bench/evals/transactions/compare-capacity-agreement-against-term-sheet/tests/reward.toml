# `reward` is the criterion pass rate across the rubric and document check.
# `all_pass` is 1 only when every criterion passes.
[[reward]]
name = "reward"
aggregation = "weighted_mean"

[[reward]]
name = "all_pass"
aggregation = "threshold"
threshold = 1.0
