# Jobs

Local evaluation output lives here and is excluded from Git.

- `raw/` contains Harbor job directories and run logs.
- `reports/` contains normalized reports opened from **Eval** in the terminal.

Task source belongs in `../evals/`.
